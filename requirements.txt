# Minimal production dependencies for the Django backend
# Keep this list lean so Render (and local dev) install quickly.

Django>=5.2.7
djangorestframework
gunicorn
whitenoise
dj-database-url
python-dotenv

django-cors-headers
pillow
opencv-python-headless

# Uncomment the line below if/when you provision a Postgres database
# psycopg2-binary

